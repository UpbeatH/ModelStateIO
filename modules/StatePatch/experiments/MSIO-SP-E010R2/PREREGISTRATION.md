# MSIO-SP-E010R2 supported-option correction

Only change from E010R1: remove the read-only-help-discovered unsupported
`--no-conversation` option. Keep the exact artifacts, prompt, seed,
temperature, token bound, CPU-only `-ngl 0`, order and static lifecycle pass
rule unchanged.
