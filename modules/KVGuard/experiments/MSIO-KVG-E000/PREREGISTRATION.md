# MSIO-KVG-E000 capability gate

Run only on g130's isolated ModelStateIO runtime after a fresh process/GPU,
binary and directory audit. Inspect the existing `llama-server` or equivalent
binary help and source-visible state APIs. If an owned, non-network request
can save and load a sequence state, require exact output equivalence, recorded
state-file size, bounded timeout and no residual server/GPU process. Do not
compare policies, modify global paths, install software, use another user's
directory or retain raw state in Git. A missing executable/API is No-Go.
